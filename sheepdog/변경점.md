# 양몰이 — 2026-09-04 손본 것

## 바뀐 파일
- index.html  : 본체 (전면 손봄)
- 양_동작.png / 개_동작.png : 32×32 칸 동작 시트 (아무 픽셀 편집기로 고칠 수 있음)
- 동작그림_만들기.py : 위 두 시트를 만드는 파이썬 (Pillow·numpy). 수치를 바꿔 다시 뽑을 때 씀
- sw.js       : 오프라인 캐시. **파일을 바꾸면 안의 `판` 번호를 올려야 폰에 새 것이 내려감**
- manifest.json : orientation landscape → portrait (게임은 세로), scope 추가

## 게임 안에서 바뀐 것
1. 속판 140×303 → 180×390. 개·양 32도트. 셈 수치는 ×1.29로 다시 잡음
2. 울타리 충돌 — 문(아래 34도트)으로만 들어감. 옆·위로 못 들어감. 개는 아예 못 들어감. 판자에 눌린 양은 개가 아래에 있으면 문 쪽으로 미끄러짐
3. 든 양은 우리 안 제자리로 걸어가 앉고, 몇 초 뒤 잠(z)
4. 동작: 양 = 가만/걷기/먹기/앉기/잠/놀람, 개 = 가만/걷기/달리기/앉기/짖기
5. 짖기 단추(오른쪽 아래, 스페이스키) — 1.1초간 도망 거리 1.7배·힘 1.6배, 식힘 2초
6. 소리: WebAudio 합성 (짖기·양 울음·들임·이김·짐). 파일 없음. 🔊 단추로 끄고 켬(저장됨)
7. 탄 10개로 유한 (1,2,3,4,5,6,7,8,10,12마리). 마지막 탄 끝내면 「다 끝냈다」 + 모은 별
8. 진행 저장(localStorage): 도달 탄, 탄별 최고 별, 소리 설정. 첫 화면에 「이어하기 n탄 / 처음부터」
9. 바탕(풀·길·나무·울타리)은 한 번만 그려 두고 씀 → 폰 부담 줄임
10. 같은 판이 두 번 돌던 것(시작 두 번 누르면 속도 2배) 막음

## 소리 (2026-09-04 추가)
- 소리/ 폴더: 배경_목장(30초 되풀이, 128bpm F장조) · 배경_첫화면(20초, 96bpm) · 이김 · 짐 · 들임 · 짖기 · 양 · 누름. mp3(게임용)+wav(원본)
- 전부 `소리_만들기.py`가 만든 원곡·합성음. 저작권 걸릴 것 없음. 멜로디는 파일 안 `멜로디` 배열(음이름, 8분음표 수)로 바꿈
- index.html: WebAudio 로 읽어 되풀이. 파일을 못 읽으면 예전 합성음으로 대신 냄. 배경 음량 .38 / 효과 .9 (`소리.준비` 안)
- 폰은 첫 손길 전엔 소리를 못 냄 → 첫 터치에 첫화면 음악 시작

## 그대로 둔 것
- 화면 비율 9:19.5, 울타리 위치·크기 비율, 흙길, 조작 방식(누르면 개가 감)

## 출시(TWA)까지 남은 것 — 코드 밖
- [ ] HTTPS로 올리기 (ledeuxions.com 아래 /양몰이/ 등). sw.js는 HTTPS에서만 붙음
- [ ] Bubblewrap 으로 TWA 만들기: `npx @bubblewrap/cli init --manifest https://…/manifest.json`
- [ ] `.well-known/assetlinks.json` 을 사이트에 올리기 (Bubblewrap이 만들어 줌) — 없으면 앱 위에 주소창이 뜸
- [ ] Play 개발자 등록 (1회 $25) — 이것이 돈이 드는 유일한 항목
- [ ] 스토어용 그림: 아이콘 512, 스크린샷 2장 이상, 기능 그래픽 1024×500
- [ ] 개인정보처리방침 페이지 (Play 필수. 수집 없음이라도 한 장 필요)

## 추정 (아직 확인 안 됨)
- 소리는 브라우저에서 못 돌려봤음. 첫 「시작」 누름에서 AudioContext 를 깨우게 해 뒀음. 실제 폰에서 확인 필요
- 후반 탄(10·12마리) 난이도는 손으로 안 해 봄. 시간 수치는 `탄들` 배열에서 바로 고침
